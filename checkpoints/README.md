# Poids du modèle final (baobab-asr-v5-t3)

Contenu attendu : model.safetensors (~2,4 Go fp32), config.json, vocab.json,
tokenizer_config.json, preprocessor_config.json. Hébergés en Kaggle Dataset
(lien : à compléter).

⚠️ vocab.json est CRITIQUE : identique de bout en bout (entraînement, réentraînements,
inférence) — le mapping des labels CTC en dépend.
